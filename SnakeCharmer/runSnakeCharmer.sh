# how to run this script
# first type "chmod a+x runSnakeCharmer.sh". This will register the script in the system.
# then execute the script with "./runSnakeCharmer.sh"

python ./SnakeCharmer.py/SnakeCharmer.py
